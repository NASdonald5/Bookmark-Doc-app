services:
  bookmark-app:
    build: .
    container_name: BOOKMARK-DOC_APP
    image: bookmark-doc-app:latest
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./data:/app/data
    restart: always
